{% extends '::base.html.twig' %}

{% block title %}
    Delete quote
{% endblock %}

{% block h1 %}

{% endblock %}


{% block menu %}
    <ul class="menu">
        <li class="first leaf rss"><a
                    href="/bash.rss?token=WJieXgVAbka5rreCz9pd" title="">rss</a>
        </li>
        <li class="leaf"><a href="{{ path('BashBashBundle_recent', { 'id': 1 }) }}">recent</a></li>
        <li class="leaf"><a href="{{ path('BashBashBundle_best') }}">best</a></li>
        <li class="leaf"><a href="{{ path('BashBashBundle_random') }}">random</a></li>
        <li class="leaf"><a href="{{ path('BashBashBundle_govnokod') }}">govnokod</a></li>
        <li class="leaf"><a href="{{ path('BashBashBundle_addquote') }}">add quote</a></li>
        <li class="leaf"><a href="{{ path('BashBashBundle_addcode') }}">add code</a></li>
        <li class="leaf"><a href="{{ path('BashBashBundle_myquotes') }}">my quotes</a></li>
        <li class="last leaf"><a href="{{ path('fos_user_security_logout') }}">log out</a>
        </li>
    </ul>
{% endblock %}



{% block content %}
    {% if (user == 'adminuser') or (user == quote.author) %}

             <div class="tabs">

                <h2 class="element-invisible"></h2>
                <ul class="tabs primary">
                    <li>
                        <a href="{{ path('BashBashBundle_post', { 'id': quote.id }) }}">

                            View


                        </a>
                    </li>
                    <li>
                        <a href="{{ path('BashBashBundle_edit', { 'id': quote.id }) }}">

                            Edit

                        </a>
                    </li>
                    <li class="active">
                        <a class="active" href="{{ path('BashBashBundle_dell', { 'id': quote.id }) }}">

                            Delete
                             <span class="element-invisible">

                    (active tab)

                </span>

                        </a>
                    </li>
                    <li>
                        <a href="/bash/node/179/rating">

                            Voting results

                        </a>
                    </li>
                </ul>

            </div>


        <form action="{{ path('BashBashBundle_dell', { 'id' : id } ) }}"
              method="post"
                {{ form_enctype(form) }} >
            <div id="edit-actions" class="form-actions form-wrapper">

                This action cannot be undone. <a href="{{ path('BashBashBundle_post', { 'id': quote.id}) }}">cancel</a>
                <div id="edit-actions" class="form-actions form-wrapper">
                    {{ form_widget(form) }}
                </div>



            </div>

        </form>



    {% else %}
        You have no rights to delete this post!
    {% endif %}
{% endblock %}